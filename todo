stop using system.exit()
leecher:82: "ArrayOutOfBoundsException" can indicate a failed transfer, but is catched and ignored. That cat was meant to allow file transfers continue if single files fail. Seems that catching IOException is a bad idea...
android-version runs out of memory when transfering large files. may be related to next point:
possible memory leak in interfacemanager. needs further investigation
